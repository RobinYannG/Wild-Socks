<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.wcs.wildcommunitysocks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MyProfil"
            android:label="PROFIL"
            android:theme="@style/AppTheme">
        </activity>

        <activity android:name=".Flux" android:label="FLUX"/>
        <activity android:name=".Classement" android:label="Classement" />
        <activity android:name=".AddPhotos" android:label="AJOUTER PHOTOS" />
        <activity android:name=".MyKickUp" android:label="Mes KickUP">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>